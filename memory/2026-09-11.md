# 2026-09-11

- **A-11 (participation cap) refused, and the tree held an unfinished iteration for the FOURTH
  time in four days** - all three sweeps (main grid + the two universe halves) had run to
  completion at 03:40-04:03 ET and nothing had been written. The right move was to verify the
  outputs against the store, add what was missing (the cost/gross decomposition and the paired
  liquid-vs-illiquid statistics), and write it up - **not** to re-run 3 hours of sweeps. Check
  `git status` *and* `ls results/` mtimes at the top of every iteration.
- **The durable result**: the sleeve's impossible fills are real (24.1% of notional fills above 5%
  of its minute, 4.1% above 100%) and **carry no gross** - clipping at 10% of the trailing median
  volume refuses $4.78M/day and costs $5/day, all of it slicing commission. The half of the
  universe whose fills *are* executable (the 8 megacaps) loses **-$195/day at t = -1.89**, and the
  sleeve's only profitable window (the 261 fitted sessions) earns **+$361/day in the illiquid half
  and -$102/day in the liquid half**. The positive evidence was always in the fills that cannot
  happen.
- **Method note worth keeping**: before believing any participation ratio, check that the store
  adjusts *volume* as well as price. The Alpaca store does - SMCI 2016-01-04 is 4.34M adjusted
  shares at $2.39 = 434k real at $23.88 - so adjusted shares / adjusted volume is the real ratio.
  If it did not, every number in A-11 would be inflated by the split factor.
- **Replay incantation (cost time again)**: `--replay` does **not** read
  `live/intraday_config.json`. A bare `python scripts/intraday_trader.py --replay 2026-09-08`
  reports 217 trades / -10,051 at `equity_frac` 1.0 and looks like a regression. The deployed
  check is `--strategy active --equity-frac 0.5 --params '<the params block from the config>'` ->
  34 trades, 368 decisions, P&L -2,302 on 500k.
- **A-12 (ORB whipsaw lockout) refused.** The tree again held an unfinished iteration - the ORB
  `reentry_block` edit and a complete `scripts/sweep_a12.py` with stage 1 already run, stage 2
  interrupted. Third time in three days that a pass died between the work and the commit:
  **`git status` at the top of every iteration is not optional.**
- **The result is a lesson about anecdote.** The 2026-09-10 paper session lost 77% of its day to
  one stopped-out-then-flipped pattern, so the obvious fix was to block the reversal. On 44,219
  round trips the reversal re-entry is the sleeve's **only profitable category** (+$15/trip against
  the first entry's -$25), and **98% of the sleeve's loss is in first entries**. Both flip cells
  lose in the paired grid; the only positive cell is the falsification control. One session's worst
  pattern is not evidence about the population - same shape as A-9's holdout lesson.
- **Cost fact to carry**: the deployed ORB sleeve is **-$331/day over 2,686 sessions**, not the
  -$289 on record before A-5 part 2 charged the US sell-side regulatory fees. Trades/day are
  identical to A-10's rows, so the whole delta is cost.
- **Runtime fact**: `sweep_a12.py` at 11 years x 6 variants x 16 names takes **~2 hours wall clock**
  with 11 workers on this box (~1.5 h of that is the four pre-2020 years). Budget a full-store
  paired grid as a two-hour job, and pipe its stdout to a file with `python -u` if progress needs
  to be watchable - plain `tee` buffers the whole run and shows nothing until it ends.
- **Ops**: live alerting is dead (`no live/alerts.json`, 15 dropped alerts on 2026-09-10). Written
  up in `BLOCKERS.md`; it needs a channel and a credential, so the loop may not fix it.

## S-2 (index-ETF opening-range breakout) - refused, and the backlog is now empty of mechanisms

- **The durable research lesson: an ORB study without its own fade control measures the stop, not
  the signal.** On SPY/QQQ/IWM, 2,687 sessions, the breakout shows gross +2.11 bps/trip - and the
  *fade* shows +0.76. Both signs cannot own a direction. Split it: `(brk + fade)/2` is **positive
  in all sixteen cells** (+0.33 to +1.43 bps) and is pure convexity - a stop plus a hold-to-close
  exit truncates losses in either direction, so a coin flip collects it. Only `(brk - fade)/2` is
  the signal, and it peaks at **+1.09 bps against a 3.65 bps round trip** and is negative in 6 of
  16 cells. **Always run the mirrored control, and always report the decomposition.**
- **Mirror the control properly.** The first implementation put an *absolute* midpoint stop on the
  faded side, so a faded short entered above the range was already through its stop and exited on
  its own entry bar 99.8% of the time - the control measured nothing. Anchor the stop as a risk
  unit measured **from the fill**, not as an absolute level, whenever a control inverts direction.
- **A landmine fixed**: `alpaca_data.py --splits` *replaced* the store's split table with only the
  symbols passed, and `--symbols` defaults to the 16-name universe while `--start` defaults to
  2024-01-01. A bare `--splits` rewrote a 60-symbol table as 16 - silently re-introducing exactly
  the per-share-commission defect A-10 spent an iteration fixing. It merges now. **Pattern to
  distrust: any writer whose argument default is narrower than the file it owns.**
- **Tooling fact**: `slippage_report.py` and anything else reading the parquet stores needs plain
  `python`, **not** `py -3.11` - the LEAN-side 3.11 has no pyarrow.
- **Runtime**: `intraday_backtest.py` on 3 symbols x ~1,000 sessions is ~11 minutes; budget ~35
  minutes for a three-regime confirmation.
- **State after this iteration**: the backlog holds **no open research item with a stated
  mechanism**. A-8 is parked by A-4's power calculation, A-3 is settled by A-10, D-2b and E-2b are
  infrastructure, S-5 needs two positive sleeves and there is one. Everything left is an owner
  decision in `BLOCKERS.md`: `equity_frac` on the intraday sleeve, `margin_budget` on the daily
  champion, the 35% drawdown cap, and the dead alert channel.

## S-14 (10:2x UTC) - breadth on the daily champion

- **Refused: more candidates make the champion monotonically worse.** LEAN full period, ranking
  sleeve 9 -> 17 -> 22 names: CAR 24.404 / 17.253 / 11.622, Sharpe 0.921 / 0.663 / 0.430, at
  realized vol 0.170 / 0.163 / 0.165. Both sub-periods lost. `evaluate.py` refuses all four cells.
- **The measurement worth keeping is about the champion, not the candidate.** With leverage, the
  vol target and the overlay switched off, the unlevered top-3 basket splits into
  `selected = pool_mean + spread`: **etf9 7.21 = 5.20 + 2.02 bps/day, t = 2.07**. That +2.02 is
  the entire cross-sectional contribution of the ranker over fourteen years, and it **only exists
  at top-3-of-9** - at `top_n=5` every sleeve's spread is insignificant (etf9 +0.84, t 1.16).
  S-9..S-14 have been tuning a component worth 2 bps a day. **S-15 (attribution) is the successor.**
- **Decompose a comparison into menu and skill before blaming either.** Of the -1.48 bps that
  separates the 17-name sleeve from the 9-name one, -0.30 is the worse menu and -1.20 is worse
  picking. Without that split the result reads as "the added ETFs are bad assets", which is wrong.
- **Vol-match before declaring a leverage effect.** Widening cut the share of funded slots carrying
  a 3x proxy from 32.7% to 17.6%, which looks like the explanation - but realized vol was
  unchanged, because the vol target levers the unlevered winners back up. Sized to the champion's
  own 0.17 std the wide sleeve still loses (22.879% / 0.872 on 8,279 orders vs 24.404% / 0.921 on
  4,735).
- **Same landmine, second tool**: `fetch_data.py` rewrote `research/data_manifest.json` whole, so a
  13-symbol fetch erased the other 69 entries (yesterday it was `alpaca_data.py --splits`). It
  merges now. **Distrust any writer whose argument default is narrower than the file it owns** -
  this is the second instance in two days.
- **yfinance**: a single-symbol `yf.download` ignores `group_by="ticker"` and returns
  (field, ticker) MultiIndex columns, so `--symbols <ONE>` crashed. Select the ticker from
  whichever column level carries it.
- **Runtime**: a full-period LEAN run of `s1_momo` is ~4 minutes; the pandas replica walk over
  3,692 sessions x 3 sleeves is ~3 minutes.

## S-15 - the champion's return attribution (11:1x UTC)

- **The headline number to remember**: the nine-ETF pool held **equal-weighted and unlevered**,
  with no ranking, no entry gate and no regime filter, run through only the vol target, margin
  budget and drawdown overlay, earns **17.282% CAR at Sharpe 0.711** against the champion's
  24.404% / 0.921. **71% of the book's return is the sizing machinery on ETF beta.** The whole
  signal stack is +7.12 CAR at t = 1.37 on 3,689 paired trading days.
- **Vol-match before crediting a concentration lever.** Removing ranking costs 6.70 CAR, but it
  also cuts realized vol 0.170 -> 0.137 (three names instead of nine). Re-sized to the champion's
  own std via `margin_budget` 0.93, the no-ranking book earns 20.745%, so ranking is worth
  **+3.66 CAR at t = 0.92**. A third of the apparent value of every "hold fewer names" result is
  leverage, and the vol target hands it back on request.
- **The regime filter's in-sample contribution is exactly zero** (-0.04 bps/day, t -0.03,
  2012-2019); all of its +2.02 CAR is 2020-2026. What it buys every regime is drawdown
  (31.4 -> 25.1) and vol (0.189 -> 0.170). Price a "crisis switch" on risk, not on return.
- **3x proxies off is better on every risk-adjusted measure**: 23.128% at Sharpe 0.950,
  DD 23.6%, std 0.153, PSR 27.4%, fees $25.6k against $45.7k, for 1.28 CAR. Same shape as L-1
  intraday: leveraged instruments supply volatility, and the vol target then removes most of it.
- **LEAN chart gotchas found building `sweep_s15.py`**: the `Strategy Equity` series is marked on
  every **calendar** day (5,358 rows, 1,669 of them zero-return weekends/holidays), which divides
  a paired bps/day by 1.45 while leaving its t unchanged; and the `Benchmark` series **cannot** be
  used to find the sessions - it is plotted one day late and repeats values, giving 2,317 "moves"
  instead of 3,690. Take the trading calendar from `lean_prices.load_closes(["SPY"]).index`.

## S-16 - instrument leverage vs account leverage on the champion (12:1x UTC)

- **The dead heat to remember**: proxies off **and** the drawdown overlay off, at the unchanged
  0.75 margin budget, earns **24.403% against the champion's 24.404%** - paired **-0.00 bps/day,
  t -0.00** on 3,689 sessions - at std 0.155 vs 0.170, DD 23.7 vs 25.1, PSR 33.2 vs 23.0 and
  $27.2k of fees vs $45.7k. Two of the champion's devices, together, are worth **zero return**.
- **Why the 3x sleeve exists at all is margin, not edge.** IBKR charges 0.333 of margin per unit
  of *economic exposure* on a 3x ETF against 0.5 on an ordinary one, so gross stops at 1 x budget
  levered (2.25x exposure) and 2 x budget unlevered (1.5x). S-15 measured the proxies with that
  exposure removed, which confounded instrument with size. **Always re-price a leverage component
  at matched risk, not at matched budget.**
- **The budget dial with proxies off is linear and Sharpe-improving**: 0.75/0.78/0.80/0.82 gives
  CAR 24.403/25.307/25.903/26.474 at std 0.155/0.160/0.164/0.168 and Sharpe 0.994/1.003/1.008/
  1.012. On the 3x book (O-1b) the same dial *cost* Sharpe. At 0.82 the unlevered book matches the
  champion's vol and beats it by **+0.66 bps/day at t = 2.02** - the S-track's first t > 2 in
  favour of a change.
- **A step drawdown breaker is harmful on an unlevered book**: at budget 0.80, shipped 0.15/0.25
  gives 24.551 / DD 25.5, widened 0.20/0.30 gives 25.333 / DD **25.0**, off gives 25.903 / 25.1.
  It costs return *and* does not buy drawdown, because flattening at -25% and re-arming sells the
  bottom (S-8's re-arming problem).
- **Three cells return "BEATS champion" from `evaluate.py` and none was promoted**, because all
  three need `margin_budget` > 0.75 and that constant is the owner's open question. The
  budget-neutral cell missed promotion **by 0.001 CAR points**. When the only thing between a
  passing candidate and the champion is an owner constant, the deliverable is a costed option in
  `BLOCKERS.md`, not a promotion.

## S-17 (13:3x UTC) - the harness charges no spread, the runner is a session late

- LEAN: `DefaultBrokerageModel.GetSlippageModel` -> `NullSlippageModel.Instance`, IB model does not
  override. `EquityFillModel.MarketOnOpenFill` applies a model when set, so it is priceable.
  New knobs `S1_SLIPPAGE_BPS` / `S1_SIGNAL_LAG`, both default 0, control reproduces
  OrderListHash 5246804e17a67af90028ffceead7d3b3.
- Spread ladder on the champion: 24.404 / 23.699 / 22.926 / 21.129 / 18.216 at 0/1/2/5/10 bp;
  drawdown 25.1 -> 32.3. Slope -0.68 CAR per bp (implies ~68x equity/yr of one-way notional).
- `scripts/daily_fills.py` (new, standing job): 6 paper fills, $1.42M, execution +2.9 bps vs the
  15:45 close (sd 16.7, se 6.8); convention vs the backtest's D+1 open -16.9 bps (se 25.8, useless).
- Signal lag proved: `ref_price` = previous close in 6/6 fills, `plan.as_of` = D-1 on both sessions.
  yfinance `period="2y"` at 15:45 ET has no bar for the current session. Priced at 19.649% / 0.736,
  -4.75 CAR, -1.55 bps/day, t -2.65; 18.592% with 2 bp of spread. Costs return, not risk (DD 23.7).
- Re-rank: S-16's (e+g) at the unchanged 0.75 budget goes -0.001 / +0.036 / +0.142 CAR at 0/1/2 bp
  vs the champion, Sharpe 0.938 vs 0.865, DD 25.0 vs 29.2, fees $24.9k vs $41.9k. Crossover ~0.03 bp.
  Return edge still t = +0.12 - the case is less risk and less cost, not more return.
- Band at 2 bp: 0.03 = +0.57 CAR (t +1.22) and -3.8 DD points vs 0.01; 0.08 = +0.09. Direction is
  evidence, level is S-13 path luck.
- Nothing shipped, nothing promoted, no default changed. Champion unchanged at S-12.
- Ops: run `slippage_report.py` on the default Python, not `py -3.11` (no pyarrow there).
- Scheduled tasks read only: "Quant Paper Rebalance" (Ready), "Quant Intraday Sleeve" (Running),
  "Quant Dashboard" (Running). None modified.

## S-18 (15:1x UTC) - PROMOTED: the champion is the unlevered book

- Shipped: `algorithms/s1_momo/signals.py` `LEVERED_PROXY = {}` (3x map kept as `LEVERED_PROXY_3X`)
  and `dd_halve = dd_flat = 9.0` (overlay off). `margin_budget` unchanged at 0.75, band unchanged
  at 0.01, signal and regime filter untouched. New `OrderListHash a6d6224ce9c70091e5bfa8e96f046bf3`
  (5,128 orders, 24.403%, 0.994, DD 23.700%, $27,199.76 at 0 bp).
- Reversibility proved: `S1_PROXY=on S1_DD_HALVE=0.15 S1_DD_FLAT=0.25` reproduces the retired S-12
  champion bit-for-bit (4,735 / 24.404% / 0.921 / 25.100% / $45,695.46, hash 5246804e...d3b3).
  Needed one fix: `margin_requirement` reads `LEVERED_PROXY_3X`, so the margin table states IBKR's
  100% on TQQQ whether or not the strategy holds it.
- Halves (candidate vs champion, S-12's own windows): IS 2012-2019 17.698 / 0.911 / 23.7 vs
  19.180 / 0.884 / 25.1 (-1.48 CAR, t -1.25); OOS 2020-2026 32.801 / 1.108 / 23.3 vs 30.863 /
  0.985 / 22.6 (+1.94, t +0.87). Return difference is OOS-weighted, risk difference is in both.
- Measurement change: `backtest.py` records the `S1_*` env with every run; `champion.json` has
  `stats_by_spread` (0.0 and 2.0 columns, each with its run_dir); `evaluate.py` compares at the
  candidate's own spread and refuses a run at a spread it has no column for. Gate said 2 bp BEATS,
  0 bp refused by 0.001 - promotion made on the 2 bp row.
- I-1 gate after the change: `compare_orders.py` 3,689/3,689, 5,021 orders both sides;
  `--mock --dry-run` plans XLE 793 / XLK 202 / IWM 211 at 1.50x gross. Expect the 15:45 ET paper
  session to sell TQQQ.
- Band on the new book: 0.03 is only +0.064 CAR at 0 bp / +0.103 at 2 bp (t +0.58 / +0.94) - a
  fifth of S-17's champion figure, because the saving was the 3x sleeve's re-weighting. Not changed.
- Standing jobs: `daily_fills.py` unchanged (6 fills, +2.9 bps, se 6.8). `slippage_report.py` got
  new input - 2026-09-11 partial session 23 fills at +1.11 bps, pooled +2.42 vs shipped 1.50,
  |diff|/se 0.99, still inside 2 se, constant untouched, ~6.1 sessions to settle.
- Not touched: `live/APPROVED_PAPER.md`, `live/HALT*`, `live/intraday_config.json`, scheduled tasks,
  `margin_budget`, `S1_SLIPPAGE_BPS` (stays 0.0).
