"""Tests for Layer B.

A package rather than a bare directory so module names are fully qualified
(``topstep_backtester.tests.test_layering``) and cannot collide with the 107 test files in
the repository root suite.
"""
