# Daily review - 2026-09-13

Window: 2026-09-12 06:30 ET -> 2026-09-13 06:30 ET. Weekend, so **no trading session fell inside
it**; the last live session was Friday 2026-09-11. Seven tracks ran, 102 commits, 481 new ledger
rows dated today (550 counting the 09-12 evening tail).

**Headline: nothing was promoted, and nothing asked to be.** `research/champion.json` is
byte-identical to `9197bd4` (S-31) and no deployed parameter moved. Eleven hypotheses were run and
**ten were refused**; the one PASS (O-7) characterises a product that had already been refused
downstream the same night. The night's real output was **four confirmed defects in the deployed
path** - one of which stopped the daily runner from starting at all for 83 minutes - and **three
method retirements**, each of which invalidates a screen this repo was about to lean on.

---

## What each track ran

| track | runs | items | verdict |
|---|---|---|---|
| `iterate` (A-, D-) | 5 | A-14, A-15, D-5, D-6 | A-14/A-15 REFUSED; D-5 and D-6 = two audit items confirmed, fixed, one material |
| `ml` (F-) | 9 | F-12, F-14, F-15, F-16, F-17 | all REFUSED; F-16 + F-17 together retire a method |
| `daily` (S-) | 10 | S-37, S-38, S-39, S-40, S-41 | no promotion; S-40/S-41 re-price the crisis switch, S-41 withdraws its own premise |
| `options` (O-) | 5 | O-5, O-6, O-7, O-8 | O-6/O-7 PASS (risk input), O-5/O-8 REFUSED; track now idle pending VALUE tier |
| `critic` (C-) | 4 | C-1, C-2, C-5, C-6 | all four targets SURVIVED; C-6 found the live import break |
| `eng` (E-) | 9 | E-8..E-11 + Phase 3A, scorecard, ledger, promotion gate | shipped; morning gate 118.7 s -> 36.3 s |
| `futures` (new scope) | 6 | Stages 1-6, multi-contract fetch, ES spread, ProjectX adapter | 544 hypotheses, **0 survivors**; branch says no |

---

## Metrics

### Refusals and their numbers

| item | claim tested | key statistic | decision |
|---|---|---|---|
| **F-17** | wider candidate pool through F-16's causal gate | net $310/day, t **+1.503** vs 2.0 bar; **below its own scramble control** ($319/day, paired -$16 at t -0.14) | REFUSED |
| **F-16** | per-year causal admission gate, 6 auction parents | $412/day at t **+1.97**, abstains 5 of 8 years - best net this track has fitted, still under the hurdle | REFUSED (retained as reference) |
| **F-15** | 19 auction-tape columns | -$148/day; 3 of them +$60 | REFUSED on the pre-registered arm |
| **F-14** | 11 bar-level order-flow columns | corr 0.65 with the 30-min return; **scrambled flow is free, real flow costs $199/day** | REFUSED |
| **F-12** | risk model vs return model | risk model 32x better fitted, 1.8x more stable, **using it makes the book worse** | REFUSED |
| **A-15** | can any magnitude nowcast size the intraday sleeve | sleeve paid **+$2,651/day per 1 sd of vol SURPRISE at t +8.41**; **-$230/day per 1 sd of the forecastable part at t -0.73**; perfect nowcast worth +$550/day | REFUSED, class closed |
| **A-14** | ORB `time_stop` 368 (the A-8 best cell) | IS 2016-2023 paired **+$565/day at t +2.93**; OOS 2024-2026 -$76 at t -0.24; median session **-$226**, 27 sessions carry **108%** of the money | REFUSED; not pure cost |
| **O-8** | 0DTE put wing forecasts the downside tail | M2 vs M1 pooled DM t **-0.401** (bar +2.576), 2/5 clocks, **declared sign wrong at 4 of 5** | REFUSED on both legs |
| **O-5** | chain's directional signal traded in SPY at 3.41 bps | 0 of 20 cells clear two-of-three; best cover **0.963** with full hindsight | REFUSED |
| **S-41** | 36-cell weight-blend of the crisis switch | fully charged FULL 18.544 CAR / 1.016 Sharpe / 23.751 DD vs shipped 19.640 / 1.047 / 24.037; paired OOS **-1.296 bps/day at t -1.64** | NOT a promotion candidate |
| **futures** | ES/NQ/MES/MNQ intraday discovery funnel | **544 hypotheses across 4 contracts, 0 survivors**; Topstep twin p(pass Combine) **0.01**, p(liquidated) **1.00**, t_HAC 0.23 vs 1.96 | branch refused |

### The one PASS, and why it does not help

| item | result |
|---|---|
| **O-6/O-7** | the chain's `rn_half` magnitude skill is incremental over the tape at **5 of 5** clocks (DM t +3.5..+4.2), present in **15 of 15** (clock x vol-state) cells, and largest in the **calm** state where a realized-vol sizer is blind. Cuts sized-book dispersion 4-14%. |
| **the catch** | its only consumer was position sizing, and **A-15 refused that class on mechanism the same night**. O-7's marginal leg (pooled calm t **+2.613** vs bar +2.576) is the one the Theta VALUE ask was cited on. |
| **O-8 secondary, and it is real** | `rn_half` cuts the OOS 5% VaR loss **-3.0% to -8.0%, rising monotonically through the day**, at 5 of 5 clocks. That is a de-risk/flatten input, which A-15 does not touch. |

### Defects confirmed in the deployed path

| id | defect | measured cost | state |
|---|---|---|---|
| **C-6** | `scripts/paper_trade.py` could not import (`quant_brain`) - the sys.path guard sat **15 lines below** the imports it protects; introduced by Phase 3A `c1fc9b1` | `Quant Paper Rebalance` = **ModuleNotFoundError exit 1**; fails closed, so a silent no-trade Monday, not a bad order. **No run was missed by ~36 h** (last ran 09-11 15:45 ok, next 09-14 15:45) | **FIXED**; gate re-run 3,689/3,689 at 5,021 orders |
| **D-5 / AUD-15** | IBKR minute store is split-adjusted; `share_scale` was 1.0 | commission understated **$548/day = 29.8% of the sleeve's whole cost line**. The IBKR-store book is **-$2,460/day, not -$1,924/day** | FIXED, 14 tests |
| **D-6 / AUD-07** | 22,081 post-market rows wearing RTH timestamps on 21 early closes in `data/minute_alpaca` (0.1494%); only 31.1% of those minutes print, at 14.3% of normal volume - the 1.5 bps charge is wrong by ~7x | deployed `active` is **bit-identical with and without them (Δ = $0.00, 0 late fills over 105 pinned sessions)**; `gap_fade` moves on 27 of 105 by mean \|Δ\| 12.5 bps of equity (max 65.8) at mean t 0.03 | FIXED on correctness; IBKR live store was already clean |
| **E-11** | morning gate blocked on 1,226 tests, 105 s of 118.7 s, in front of a 09:30 open | the full suite's entire contribution across 11 repo shapes was **one row - a refusal caused by a missing marker**, i.e. a self-inflicted outage | gate is now `-m runner` on its own budget; **to-decision 118.7 s -> 36.3 s** |

---

## Promotions and refutations

- **Promotions: none.** `champion.json` untouched all night. `live/intraday_config.json` untouched
  (still `active`, `equity_frac` 0.25, ORB-only).
- **Refutations of this repo's own prior claims** - five, and they are the most valuable rows here:
  1. **S-41 withdraws the sentence S-41 was opened on.** "The blend carried the best Sharpe 1.173,
     beating the shipped cell's 1.159" compared a **2,684-session** row against a **3,689-session**
     row. Span-matched, the shipped cell is **1.228** and the blend **1.174**. The blend never had
     the best Sharpe. Second instance this month of that exact shape (S-40 corrected S-38).
  2. **F-17 retires the F-14+F-15 gate.** A train-window \|IC\| floor is an **anti-selector** across
     stores with different regime exposure: the five admissions with the largest in-window \|IC\|
     (\|t\| 5.7-7.7) all lost; the three weakest (\|t\| 2.1-2.9) made all the money, **and the
     ranges do not overlap**. The whole flow result is one regime break (2020: -$974/day at t -1.90).
  3. **A-15 finds its own premise wrong.** The sleeve has no vol sizing to beat - `orb` is a
     constant 0.12 of equity. The incumbent is flat notional, so the control had to be built.
  4. **D-6 corrects AUD-07's own sentence.** "`late_momo` opens at 15:00 in the post-market tape"
     was true when written and was closed on 09-12 by `eng`'s calendar-aware `flatten_minute_for`
     (returns 188 on a 13:00 close). **The store never changed; a guard in a different file did.**
  5. **Root `BLOCKERS.md` nearly lost OWNER-5.** The new file claimed to hold everything needing the
     owner and contained zero mentions of Theta or the VALUE tier. Restored as OWNER-5.

---

## The critic's verdicts

Four targets, **four survivals** - and in each case the collateral finding outweighed the verdict.

| target | verdict | the correction that stands |
|---|---|---|
| **O-7** (C-6) | **SURVIVES** | O-7 disqualified `rv_sofar` as a splitter for being a fitted input, then used `rv20`, which is also one. Re-run under three **non-fitted** causal labels: `rv60` t **+3.288**, `gapvol20` **+2.848**, `rng20` +2.180 - the two furthest from the fitted set are the **strongest**, so contamination biased O-7 *against* itself. Block-bootstrap p = 0.0003-0.0024. Wording fix: the pooled calm t ranges **+2.03 to +3.29 and straddles its bar**; leg (ii) (15/15 cells) is what carries the claim, and that is what the subscription ask must cite. |
| **S-37** (C-5) | **SURVIVES** | its own reproduction script cannot run on the code it shipped; the headline is quoted from a cell at 11x the outage rate it describes; the shipped gate is one-sided. |
| **A-13** (C-2) | **SURVIVES** (5 attacks) | the pooled gain is the fill fix, not nav; clause 4's own 10% threshold fails at **14.65%** on the window where the defect can act; the owner-facing tail is **4x too large**. |
| **S-31** (C-1) | **SURVIVES** (4 attacks) | its one owner-facing recommendation does not: a 25% drawdown cap asks for **no change**, not for 0.70. |

**C-6's third rule is the one to keep: re-quoting a gate number is not running a gate.** Two commits
(`0515f3b`, `0193f21`) reported "compare_orders.py 3,689/3,689" 83 minutes *after* the command had
stopped being able to start. The numbers were right. The gate was dead.

---

## Paper session P&L by sleeve

No session in the window (Sat/Sun). Last session **Friday 2026-09-11**, account `DUT091359`:

| sleeve | P&L | trades | costs | end state |
|---|---|---|---|---|
| **intraday** (`active`, ORB-only, `equity_frac` 0.25) | **-$201.99** | 34 | $57.95 | flat at close, 0 positions, 21 decisions, ended 15:42 ET |
| **daily** (`s1_momo`, `Quant Paper Rebalance`) | no trade in window | - | - | last real run 09-11 15:45 result 0; next **09-14 15:45 ET** |
| account | NAV 986,287 at intraday start; NLV 988,032 on the 09-12 dry-run read | | | |

Relaunched once on 09-11 at 13:33 UTC to move `equity_frac` 0.5 -> 0.25 mid-session (owner-pending
size decision). Weekend logs hold preflight/replay traffic only: 09-12 = 12 `preflight_tests`,
09-13 = 7 `preflight_tests`, 6 `preflight_store`, 3 `suite_report`.

**Two gate failures logged, both traceable to cross-track contention, both now clear:**

- 09-13 08:49 UTC - preflight **FAILED, refused to trade**: 5 collection errors from the futures
  track's half-written test files. This is the E-8 defect **inside the gate's own file**; E-11 now
  reads the *selection* rather than the exit code and reproduces the failure deterministically.
- 09-13 08:55 UTC - full suite failed off the trading path (`test_qb_multipletest.py`, `NameError:
  DSR_BASE`) while `runner` passed. Also a mid-write artifact.

**Verified at HEAD this morning:** `test_qb_multipletest.py` 77 tests green; `pytest -m runner`
**230 tests green**; `paper_trade.py --help` **exit 0**. Monday's 09:25 gate is clear as of 06:30 ET.

---

## Open blockers

Root `BLOCKERS.md` now supersedes `research/BLOCKERS.md` (last reviewed 2026-09-13).

| id | status | what is needed |
|---|---|---|
| **OWNER-1** | OPEN, blocks buying an evaluation | three Topstep numbers off your own dashboard: Combine profit target (currently SEARCH tier), consistency threshold (the article contradicts itself), monthly cost (not found anywhere - so the twin reports gross only, labelled `FEE UNKNOWN`). Five minutes. `combine()` raises `UnverifiedRule` until then, deliberately. |
| **OWNER-2** | OPEN, blocks unattended operation | nothing pages you when the sleeve breaks. Every alert written in the last three days carries `"delivered": false, "error": "no live/alerts.json"`. The system can explain itself to someone who looks; it cannot get someone to look. |
| **OWNER-3** | OPEN, binding on the whole futures branch | breadth turned out not to be depth. Four contracts, but the deepest is **395 sessions against the ~2,000** A-4 measured as necessary, and no 2020 or 2022 in any of it. ES `BID_ASK` did retire one assumption: spread is a measured median of **exactly 1.00 tick in RTH (95.6% of bars)**, so the 0.480 bps cost model agrees with the tape and with F-2a's 0.488. **One free step left that does not need you:** probe whether a free QuantConnect account has deeper CME history than IBKR. |
| **OWNER-4** | OPEN, blocks execution not research | no Topstep account, so no prop-firm adapter can be exercised. A ProjectX/TopstepX adapter was built last night **structurally unable to place a live order**; it is untested against a venue. |
| **OWNER-5** | OPEN, options track idle | Theta has lapsed to FREE (`HTTP 403`). **The old justification has lapsed too** - the VALUE ask was partly cited on O-6's PASS being handed to the A-track, and A-15 refused that class on mechanism. O-8's VaR result is a candidate fresh reason; it has not been costed. Until then the O-track has no open item and `research-options` is spending tokens on an empty scope. |
| **daily runner's clock** | OPEN, ~1.9 CAR points | the pre-open move (breakeven +3.10 bps of extra opening-auction cost; the loop's half is merged and gated). One scheduled-task change. |
| **intraday `equity_frac`** | OPEN | the sleeve is negative in every regime after costs on 2,686 sessions. It runs at quarter size as a plumbing test only, pending your decision on whether it trades at all. |

---

## Plan for the next 24 hours

1. **`eng` - C-7 first.** Launch `paper_trade.py` and `intraday_trader.py` as **subprocesses** in a
   `runner`-marked test, with the scheduler's own interpreter and cwd and **no conftest path help**.
   C-6's break hid behind 1,221 green tests because `tests/conftest.py` inserts `REPO` and the
   scheduled task does not. One file, no data, closes an outage class.
2. **`eng` - the provenance rule, and it is now the loop's biggest process defect** (see usage note).
   **Five** commits yesterday carried another track's staged files, and the fifth touched
   `scripts/intraday_common.py`, which the live trader imports. `AGENTS.md` step 7 forbids
   `git add -A`; the missing half is that after clearing a stale `index.lock` you must inspect what
   is already staged. Make the pre-commit hook refuse a commit whose staged set exceeds the paths
   the author named.
3. **`ml` - F-19 before F-18.** Every F-track panel predates D-6's calendar trim, so
   `data/f1/panel.parquet`, `f14.FLOW`, `f15_famA` and `PREDS` all carry post-market bars resampled
   into 5-minute bars that never existed. Only 0.9% of rows, but F-1's labels are **forward**
   returns, so a bogus bar contaminates an earlier real one's label. Rebuild, then re-run F-16's
   auction arm as the identity check. F-18 is not worth fitting on a contaminated panel.
4. **`options` - do not schedule a new hypothesis.** The scope is empty until VALUE is restored. The
   one exception worth one run: **cost O-8's VaR result as a de-risk/flatten switch**, since that is
   the only consumer A-15 did not close and it would give OWNER-5 a fresh reason.
5. **`futures` - take the free step in OWNER-3** (QuantConnect CME depth probe) before any further
   discovery runs. 544 hypotheses and 0 survivors on 395 sessions is the data speaking, not the
   method; more funnels on the same 1.25 years will keep producing zeros or a lookahead.
6. **`daily` - S-42** as filed: the ensemble trick on the axes S-38 labelled FITTED that are *not*
   risk dials, where a CAR grid is a fair instrument. Do **not** build the shipped 36-cell blend -
   it needs a `signals.py` change plus a LEAN run for a quarter of a drawdown point.
7. **`critic` - C-4** (S-24, the pre-open MOO claim), queued and twice deferred. It is the claim
   behind the ~1.9 CAR-point owner ask in `BLOCKERS.md`, so it should be attacked before you are
   asked to move a scheduled task on it.
8. **`iterate` - A-16**, the latent twin of D-5: `intraday_trader.py`'s **replay** path still costs
   the adjusted store as raw. D-5 fixed the harness; the replay preflight is the gate that decides
   whether the sleeve trades on Monday.

---

## Usage note

| | count |
|---|---|
| commits in window | **102** (37 since 00:00 ET today) |
| job runs, by commit clustering (>45 min gap = new run) | **~48** across 7 tracks: eng 9, daily 10, ml 9, futures 6, iterate 5, options 5, critic 4 |
| new ledger rows | **481** dated 2026-09-13 (550 including the 09-12 evening tail); 1,432 total, plus 818 in `experiments_futures.jsonl` |
| runs that hit a token cap or a rate limit | **0 recorded.** No journal in the window names a rate limit or a partial-result resume. |
| runs plausibly cut off mid-commit | **2** - `fcef1b1` "kgkku" and `11f0308` "vcjikyftr", both carrying real diffs under a garbled message |
| **commits carrying another track's staged files** | **5** - `560b0bb` (S-39 files), `0193f21` (S-39), `3a7b764` (C-6's four files), `98512df` (the futures multi-contract fetcher + 408 ledger lines), `0515f3b` (`sweep_o7.py`). **Plus `11f0308`: the Memory Dreaming cron committed D-6's `scripts/intraday_common.py` calendar-trim fix under the message "vcjikyftr", 97 minutes before the `iterate` track's own D-6 commit.** |
| suite growth, one Sunday | **1,171 -> 1,470 tests in ~30 h (+25% in four hours)**; full suite 81.6 s -> 144.7 s. The 230 `runner`-marked tests are flat at ~19 s, which is why E-11 split them. |

The provenance number is the one to act on. Six commits in one night put files under the wrong
track's message, the tracks caught it themselves each time and wrote it down rather than rewriting
history - which is the right call - but the sixth instance moved a file the live trader imports, and
that is no longer only a bookkeeping problem.
